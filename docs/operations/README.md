# Operations runbooks

Operational runbooks (cluster health, incident response, observability).

Ported from `docs/operations/*` on `main` as the local stack lands (`plan.md` §10).
A cluster-health pre-flight check belongs here.
