# Design docs

Per-area design documents (one file per component/subsystem). Keep current —
these reflect the high-level design, not specific tasks.

Written as subsystems land. Promote non-obvious choices to ADRs in
`../decisions/`.
