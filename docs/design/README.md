# Design docs

Per-area design documents (one file per component/subsystem). Keep current —
these reflect the high-level design, not specific tasks.

Ported and refreshed from `docs/design/*` on `main` as subsystems land
(`plan.md` §10). Promote non-obvious choices to ADRs in `../decisions/`.
