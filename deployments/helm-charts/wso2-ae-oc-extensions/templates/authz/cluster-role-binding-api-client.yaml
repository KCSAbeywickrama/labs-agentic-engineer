# Grants the aep-api service account admin access to OpenChoreo so it can
# create/manage projects, components, workflows, and deployments on behalf of users.
apiVersion: openchoreo.dev/v1alpha1
kind: ClusterAuthzRoleBinding
metadata:
  name: aep-api-client-binding
spec:
  effect: allow
  entitlement:
    claim: sub
    value: {{ .Values.authz.apiClientId }}
  roleMappings:
    - roleRef:
        kind: ClusterAuthzRole
        name: admin
