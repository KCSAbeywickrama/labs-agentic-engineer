aepApi:
  replicas: 1
  image:
    repository: ghcr.io/wso2/aep/aep-api
    tag: latest
    pullPolicy: IfNotPresent
  port: 9090
  logLevel: "info"
  deploymentTier: "dev"
  resources:
    requests:
      cpu: "100m"
      memory: "256Mi"
    limits:
      cpu: "1"
      memory: "512Mi"
  serviceAuth:
    clientId: "aep-api-client"
  publicURL: "http://aep-api.openchoreo.localhost:8080"

aepAgents:
  replicas: 1
  image:
    repository: ghcr.io/wso2/aep/agents
    tag: latest
    pullPolicy: IfNotPresent
  port: 9090
  logLevel: "info"
  model: "claude-sonnet-5"
  maxSteps: 20
  bodyLimit: "10mb"
  resources:
    requests:
      cpu: "100m"
      memory: "256Mi"
    limits:
      cpu: "1"
      memory: "512Mi"


console:
  replicas: 1
  image:
    repository: ghcr.io/wso2/aep/console
    tag: latest
    pullPolicy: IfNotPresent
  port: 3000
  resources:
    requests:
      cpu: "100m"
      memory: "256Mi"
    limits:
      cpu: "1"
      memory: "512Mi"
  publicURL: "http://console.openchoreo.localhost:8080"
  thunderPublicURL: "http://thunder.openchoreo.localhost:8080"
  thunder:
    clientId: "aep-console-client"
    scopes: "openid profile email"

taskSigning:
  issuer: "aep-bff"
  audience: "git-service"

observer:
  clientId: "openchoreo-observer-resource-reader-client"

thunder:
  # In-cluster URL for the Thunder service. Override if Thunder is deployed in a different namespace or port.
  adminURL: "http://thunder-service.thunder.svc.cluster.local:8090"
  jwksURL: "http://thunder-service.thunder.svc.cluster.local:8090/oauth2/jwks"

openbao:
  # Address of the OpenBao service. Matches: helm install aep ./bundle -n wso2-aep
  addr: "http://aep-aep-openbao.wso2-aep.svc.cluster.local:8200"

# Shared git workspaces (RWX PVC) — bare repo mirrors + per-SHA snapshots.
# aep-api is the sole writer; agents mounts read-only.
# See docs/design/shared-volume-clone-architecture.md §13.
workspaces:
  storage: "10Gi"
  # StorageClass for the ReadWriteMany claim. Empty = cluster default.
  # A real RWX class (NFS/EFS/CephFS) is required for multi-replica aep-api;
  # single-node k3d fakes RWX on local-path, which is fine locally.
  storageClassName: ""

postgres:
  version: "16"
  database: aep
  user: aep
  storage: 1Gi
  resources:
    requests:
      cpu: "100m"
      memory: "256Mi"
    limits:
      cpu: "500m"
      memory: "512Mi"