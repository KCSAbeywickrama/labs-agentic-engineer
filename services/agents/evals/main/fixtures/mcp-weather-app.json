{
  "name": "mcp-weather-app",
  "description": "MCP discovery (Task E2): the org already has an `openweather` external resource registered (mock catalog in evals/mocks/mcp-server.ts). The agent should call list_external_resources before authoring the weather dependency and reuse that exact name + config-key schema instead of inventing a new one.",
  "difficulty": "medium",
  "turns": [
    {
      "prompt": "Derive a new backend component named `forecast-api` — a Go service that fetches the current weather from a third-party weather provider and returns it to callers. Follow our design skill for the design.json shape, and check what external resources this org already has registered before declaring the weather dependency."
    }
  ],
  "expect": {
    "toolsUsed": ["list_external_resources"],
    "fileMatches": [
      {
        "path": "specs/design/components/forecast-api/design.json",
        "pattern": "\"kind\":\\s*\"external\"[\\s\\S]*?\"name\":\\s*\"openweather\""
      }
    ],
    "filesContain": [
      { "path": "specs/design/components/forecast-api/design.json", "text": "OPENWEATHER_API_KEY" }
    ],
    "touched": ["specs/design/components/forecast-api/design.json"],
    "noToolErrors": true
  }
}
