{
  "name": "mcp-platform-resource-database",
  "description": "MCP discovery (Task E2): the cluster already has a `postgres-cnpg` resource type installed (mock catalog). The agent should call list_platform_resource_types before declaring a database dependency and reuse that exact resourceType instead of inventing one.",
  "difficulty": "medium",
  "turns": [
    {
      "prompt": "Derive a new backend component named `orders-api` — a Go service that persists orders in a relational database. Check the platform's registered resource types before declaring that dependency."
    }
  ],
  "expect": {
    "toolsUsed": ["list_platform_resource_types"],
    "fileMatches": [
      {
        "path": "specs/design/components/orders-api/design.json",
        "pattern": "\"kind\":\\s*\"platform-resource\"[\\s\\S]*?\"resourceType\":\\s*\"postgres-cnpg\""
      }
    ],
    "touched": ["specs/design/components/orders-api/design.json"],
    "noToolErrors": true
  }
}
